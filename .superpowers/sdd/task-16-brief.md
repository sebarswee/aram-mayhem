# Task 16: Update UI Components

## Goal
Update UI components (HUD, SkillSelectUI, UpgradeSelectUI) to display element information and new synergy effects.

## Files
- Modify: `src/ui/HUD.ts`
- Modify: `src/ui/SkillSelectUI.ts`
- Modify: `src/ui/UpgradeSelectUI.ts` (if exists)

## Requirements

### 1. HUD Element Display
- Show element icons for each skill
- Display synergy notifications when triggered
- Show element resistance stats

### 2. SkillSelectUI Element Colors
- Use correct element colors from `src/data/elements.ts`
- Display element name/type for each skill

### 3. UpgradeSelectUI Enhancement
- Show element restrictions for enhancers
- Display element compatibility

## Testing
- Run `npm run build` to verify no TypeScript errors